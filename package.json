{
  "name": "discord-bot-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:bot": "pnpm --filter bot dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "register": "pnpm --filter bot register",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
